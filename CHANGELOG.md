# Changelog · Price Range Dashboard

Author: Xuhao Chao · License: MIT

## v18.0 (2026-08-06) —— 压力位引擎按持有期重写;一个偷看未来的期权墙;以及一次什么都没搜到的调参

这一轮交付的东西里,**面板上的功能是减少的**:0–100 的强度进度条没了,「支撑位会不会守住」
没了,可达性概率也不再印百分比。三样都不是做不出来,是做出来之后被自己的回测否掉了。
下面按发生的顺序记,不按好看的顺序记。

### 重写:持有期是第一维,不是事后能加的一个参数

旧引擎的每一个长度都写成百分比:合并容差 1.5%、带宽下限 1.5%、固定 48 个箱。
百分比对高波动票太严、对低波动票太松 —— 同一个 1.5% 在 NVDA 和 KO 上表达的根本不是同一件事,
而这件事在"一个月内够不够得着"这个问题上是决定性的。

新引擎把所有长度都换成 `1u = σd·√h·P`:σd 是日对数收益标准差,h 是持有期的交易日数
(短 5 / 中 21 / 长 63),P 是 as-of 那天的收盘价。于是长中短期不是三套算法,
而是同一把尺子的三个刻度 —— 同一个位置在长周期上不确定性更大,带子就更宽,
比例是 √5 : √21 : √63 ≈ 1 : 2 : 3.6。持有期因此必须在设计的第一步就进来:
它不是一个可以事后乘上去的系数,它决定了每一条带的边界落在哪。

同时删掉的三件事,每一件都有具体的否证:**0–100 强度分**与后续走势的相关是 r ≈ +0.01,
而它在界面上是一根进度条,进度条这个隐喻本身就在宣称"越长越可信";
**守位预测**用三套不同对照测过,max|z| 落在 1.06–1.20,门槛是 2,三份提案得到同一个"没有";
**估值线**被 D 组回测判为 `valAnchorBias = biased`(中枢相对现价的偏离中位数为负、方向命中率跌破 50%),
一条被自家回测判为系统性挂偏的线不该继续占"压力位"这个名分,它降级成图上的虚线参考,
永不进上下两张表、永不参与合并。

少了一个功能而不说,用户会以为它还在。所以「不预测守不守得住」这句话是压力位表下方的**常驻正文**,
不是 tooltip。

### 事故:期权墙读的是几周之后才登记下来的未平仓量

`optionWalls()` 从来没有按 `asof` 过滤过期权行。`Assets/options/` 里的链只覆盖一个很窄的近期窗口
(NVDA-US 是 2026-07-29 ~ 08-06),而回放会把参照日拨到几周甚至一年前 ——
于是站在 2026-07-01 那天读到的,是 08-06 收盘之后才登记的 OI。
**一份在参照日之后拍下的快照不是证据,是答案本身。** 拿它当"当时看得见的信息",
等于先看了后面的走势再回头画墙,画出来的墙当然处处应验。

这个 bug 的形态和 v17.1 那次财年错配是同一类:不抛异常、不打日志、数字全在合法范围。
它被抓住,靠的是**两条路径给出两种世界**这个矛盾 —— 线上 `breakoutBuy` 在 NVDA 上触发 4 次,
五个候选位置的 `tracks` 全是 `["opt"]`;而回测那边从不往 `state.options` 里塞链,
同一条规则一次都不触发。同一份代码,一边有 4 次突破,一边一次都没有,差的就是这一行闸门。
闸门补上之后,那 4 次触发**全部消失**——它们从头到尾就不存在。

同一个函数里还修了一处同型的:行权价窗口原来围着 `co.price`(永远是**今天**的价)取,
这让 `refISO` 对这个函数完全无效 —— 回放到一年前,窗口仍以今天的价为中心,
一年的涨幅足以把整条链推出 ±25% 之外,于是那天"一面墙都没有"。现在两处都用 `asOfPrice()`。

判据用 `>` 不用 `>=`:参照日**当天**收盘后登记的 OI 是那天收盘时就存在的存量。
`refISO` 为空(线上看今天)时不设限 —— 那时候没有"未来"可偷。

### 台账:164 行盖作废戳,不删、不改、不结算

污染引擎(`engine_ver = 2a80ddb1`)已经往 `Assets/_logs/forward-ledger.csv` 里写下了 164 行前瞻预测。
这批行不能删:删了哈希链就断,而且"删掉不利证据"本身就是这份台账最该防的动作。
也不能让它照常结算:结算出来的胜率是用未来数据换来的。

所以走第三条路 —— **留着,盖戳,拒结**。`engine-void.csv` 里追加一行
`2026-08-06T10:20:37Z | 2a80ddb1 | 前瞻污染:期权墙未按 asof 过滤 | 164`,
`--resolve` 从此拒绝结算这个分区的任何一行。作废簿自己也是只追加 + 哈希链,
盖过的戳同样撤不掉、改不动。

引擎版本分区(`engine_ver` = 引擎源文件拼接的 sha256 前 8 位)本来只解决"新旧不混统",
解决不了"旧的那批本身是脏的"。这次是作废簿第一次被真的用上。

### `PX_EVIDENCE` 原来是一张贴纸

`PX_EVIDENCE` 这张表本该是渲染层的**开关**:某条腿判了 pending,面板上那一列就不该出现百分比。
实测发现它是**装饰**:引擎那一行写死成 `L.opts.length ? 'pending' : 'verified'`,
把 `PX_EVIDENCE.reach` 翻成 `'pending'` 再重渲染,得到的是
`{before:'已验证', after:'已验证', changed:false}`。

一个改了没反应的开关不是开关。而 SPEC 3.9 的兜底条款("任何一格判为未过,该腿的 evidence 落为 pending")
全靠这根线执行 —— 线断了,那条款就是一句没有执行机构的法律。

现在 `evidence` 是 `PX_EVIDENCE` 的函数,回测判语一变,徽章和百分比跟着变,不需要谁记得来改代码。
`[14]` 节补了一条**端到端 DOM 断言**钉住这根线:改常量 → 重渲染 → 数真实表格里的百分号,
翻成 pending 后整张压力表里一个百分号都不剩。这条不许降级成对字段的单元断言 ——
字段对了而渲染没跟上,正是原来的那个缺陷。

### 两处降级,都是把面板改得更悲观

`reach: 'verified' → 'pending'`。G 组三条预注册判据里,可靠性图 10 桶最大偏差
0.2213 / 0.2035 / 0.2648(h = 5/21/63)全部超过门槛 ≤0.10,三格全灭。
位置排序大致对(样本外 Brier skill 一直是正的),但**刻度不对** —— 说 40% 的地方实际发生了 65%,
这种数印出来比不印更坏。已知存在一个结构性争议(`PX_REACH_U = 1.0` 给 `pReach`
压了 0.0956/0.2449/0.3269 的硬下限,最低那 2–3 个桶永远填不满,门槛可能根本够不到),
但那是**下一轮预注册前**该提的动议,不是这一轮看完数据再回头改门槛的理由。先降级,再论证。

`bounce: 'falsified' → 'inconclusive'`。B 组实测 z = −0.35 / −0.76,effN 17 / 16,
而预注册要求 effN ≥ 30。**样本不足不给人宣告证伪的权利** ——「测了没过」和「根本没测够」
是两件事,前者是结论,后者是空白。面板正文一直说的是「测不出」,这里只是把徽章改成同一个意思。

### 判据本身也修了两处

**三条并联,不是两条。** 3.9 给 G 组预注册的是三条判据 AND 在一起:样本外 skill 过门槛、
CV 下界 > 0、可靠性图最大偏差 ≤ 0.10。代码里那一行以前只 AND 了前两条,第三条被丢到下面单独打印 ——
于是 h=21 那格 skill 过线、maxDev 0.21 不过线,`ok` 却是真。门槛一个字没改,只是终于都被算进同一个判定里。

**CV 下界的临界值从 1.96 改成 t(df)。** 1.96 是**已知总体方差**时的正态临界值;
CV 下界那里的标准差是拿 k 个折内 skill 现算出来的,自由度只有 k−1。k=10 时正确的值是
t(9) = 2.262,比 1.96 大 15%。折数是 min(10, 票数),票越少 df 越小、t 越大 ——
用常数 1.96 等于票越少门槛越松,而票越少本该越保守。df > 30 之后 t 与正态差 < 2%,回落到 1.96。

### 调参:搜了两轮六格,一格没过,三个参数原地不动

许可范围就是 3.3 参数表「搜索范围」栏写明的那三个,一个不多:
`PX_SIGMA_WIN` {20,40,60,120}、`PX_HALFLIFE_D` {45,90,180,365,∞}、`PX_REACH_C` 各 ±0.15 步长 0.05 共 7 档。

评分必须是**嵌套**的,这一点预注册时没写清楚,得补一句理由:`reachProb` 在运行期零拟合参数,
所以 G 组原有的那个"样本外"划分实际上只是逐折重估了气候基准。**一旦拿折上的结果去挑 `PX_REACH_C`,
c 就变成了拟合参数**,那个划分立刻不再是样本外。所以外层留一票用于评分,
内层在 9 只训练票里再做一次留一来挑配置,留出的那只票不参与任何挑选。
这样评的不是"某个配置",而是"**搜索这个动作**"—— 后者才是下一轮真要用的东西。
对照臂 = 同一套流程但候选池只有初值一个(= 根本不搜索),两臂之差就是搜索的净贡献。

| 轮次 | 持有期 | 候选数 | 对照 | 处理 | 净贡献 | CV 下界 | maxDev | 判定 |
|---|---|---|---|---|---|---|---|---|
| 全网格 | 5 | 105 | 0.2428 | 0.1679 | −0.0750 | −0.031 | 0.22 | 未过 |
| 全网格 | 21 | 105 | 0.1093 | 0.2642 | +0.1549 | +0.133 | 0.21 | 未过(skill 过线,maxDev 不过) |
| 全网格 | 63 | 105 | 0.3859 | 0.2891 | −0.0969 | −0.113 | 0.20 | 未过 |
| 只搜 c | 5 | 7 | 0.2428 | 0.2371 | −0.0057 | +0.030 | 0.13 | 未过 |
| 只搜 c | 21 | 7 | 0.1093 | 0.0876 | −0.0217 | −0.057 | 0.21 | 未过 |
| 只搜 c | 63 | 7 | 0.3859 | 0.3952 | +0.0093 | +0.010 | 0.17 | 未过 |

`PX_HALFLIFE_D` 那一轮值得单独说一句,因为它差点被读成"有效应":单参数扫描的样本外极差是
0.2055 / 0.1830 / 0.1762,**远超**预注册那条「极差 < 0.005 判 no_effect」的线 —— 那条规则根本没触发。
判 `no_effect` 靠的是另外两条,都比极差规则硬。其一,那个极差不归半衰期:h=5 上 skill 从 0.037
单调升到 0.243 的同时,平均带宽从 1.553u 单调升到 2.023u、样本量从 334 掉到 253 ——
换一档半衰期就换一批观测、换一套带宽,而带宽单独就能把命中率从 48% 推到 86%。
**这条曲线量的是带宽,不是半衰期。** 其二,嵌套评分上它不赚钱(见上表全网格三行)。
卷宗里那条「45d > 90d > 180d > 365d」的单调扫描没有复现,本轮三个持有期上 365 与 ∞ 都排在 45 前面,
方向是反的。

按 3.9 兜底条款:三个参数一律取回初值 —— `PX_SIGMA_WIN = 60`、`PX_HALFLIFE_D = 365`、
`PX_REACH_C = {5:0.60, 21:0.86, 63:1.02}`。**没有回头改参数,没有回头改门槛,没有第三轮。**
裁决行已写进 `Assets/_logs/param-adjudication.csv`,它和作废戳一样撤不掉 ——
防的是半年后有人再搜一遍、挑一个当时最好看的值,并且真心以为自己是第一次搜。

机器核对过一遍:3.3 表里 25 个常量,`params.js` 里 24 个逐字节相同,
唯一不同的是 `PX_EVIDENCE`,而它那一栏写的就是「由回测结果更新」。

### 顺手修的一处排版,以及为什么它不算小事

买入模拟面板里,处理组胜率是 21px/650 的近黑,随机对照是 11px/400 的灰,置信区间 10.5px 的灰。
三个数确实"并排"了,`sim.css` 第 ① 条的字面要求算是满足;可同一句话里还写着「同样的字号量级」,
而 21 : 11 : 10.5 不是同一个量级,是主标题配脚注。

代价是具体的:`supportBuy` 的处理组 45.0% 对上对照 62.5%,z ≈ −0.93;
拿 1260 个样本的无条件 5 日胜率 52.4% 做基线,处理组根本没跑赢过什么。
再把对照重抽 200 次,它自己的 sd 就有 7.9pp —— 那个 62.5% 是一次 +1.3σ 的抽签。
**这一格里唯一被排版捧成"结论"的数字,恰恰是三个数里最没有内容的那个。**

现在三行共用同一条 CSS 声明(13.5px / 600 / `--text-primary`),字号、字重、颜色一起写死,
谁都不能单独长大。`[13]` 节加了一条断言两头钉:computed style 证明渲染出来真的相等,
样式表文本证明它们共用同一条声明 —— 只验前者,三条各写各的、恰好写成一样,下一次改动就会静默分家。
一个计算值都没改,改的只是谁看起来像结论。

### 本轮已知未修

`PX_SIGMA_WIN = 20` 在这个仓库里跑不出来、`ingestOptions` 让 ΔOI 架构上不可得、
`[8]` 的三条断言挂在 2026 年 9 月之后就会过期的到期日上、3.9 的兜底词表里没有 `inconclusive`
这一档(而 bounce 恰好落在那里)、TSM 页头 402.9 与期权步 374.67 对不上 ——
五条都记在 [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) 的「已知缺陷」一节里,附着为什么本轮不动它们。

## v17.1 (2026-07-31) —— 财年认表里的标签,不认文件名;并加一条口径护栏

你问的是这个:「在检查下 bug 和 NVDA 的逻辑 是不是哪里出错了?」
是真错了,而且错得很难看:**NVDA 的中枢连正负号都是反的**。

### 现象

那张卡片自己在打自己:左边写「当前 P/E 12.9x,处在历史 4% 分位」——便宜到近三年最低;
右边写「中枢 162.9,现价比中枢贵 16.5%」——贵了。同一支股票,同一时刻,一便宜一贵。

两句话都没算错,只是**算的不是同一件事**:12.9x 那条分位曲线来自 `Jan '29E`(第三个财年),
162.9 里的 EPS 来自 `Jan '27E`(第一个财年)。三年后的盈利预期自然比明年高,除出来的 P/E 自然就低,
低到"历史 4% 分位";再把这个低分位乘回明年的 EPS,中枢当然被压下去。两套口径拼在一张卡片上。

### 根因:一句 `if` 里的一个假设

`src/js/ingest/estimates.js` 判定这份 Estimate History 跟的是第几个财年,用的是文件名:

```js
const isFY2 = /FY\s*2/i.test(String(fileName || ''));   // 老代码
```

只问"文件名里有没有 FY2",答否就当第一财年。于是 `NVDA-US FY3 Estimate History.xlsx` ——
一个名字里明明白白写着 FY3 的文件 —— 被当成 FY1 收了进来。

后面两条写入路径的胜负规则又刚好相反,把错误固化了下来:
EPS 是后写的赢(FY1 后加载,EPS 留下的是 FY1 的),
P/E 分位库却是 `if (peSeries.length > existing.length)` 长的赢,两边截断到 24 个点之后 `24 > 24` 为假,
先加载的赢(FY3 文件 mtime 更早,分位库留下的是 FY3 的)。**FY3 的分位 × FY1 的 EPS**,正是屏幕上那两个数。

那五个 FY3 文件是 2026-07-27/28 两轮抓取落在了错误的财年页签上留下的。
`fetcher/lib/registry.mjs` 只请求 FY1 和 FY2,所以它们本不该存在;而 v17.0 的落榜清理永远不会碰它们 ——
这些代码都在拉取清单里,清理只看清单不看财年。

### 错到什么程度

四家真实数据,左边是错配时屏幕上显示的,右边是配对正确后的:

| | 显示的中枢 | 正确的中枢 |
|---|---|---|
| NVDA-US | 162.9(**−16.5%**) | 218.3(**+11.9%**) |
| GOOGL-US | 300.4(**−10.0%**) | 386.1(**+15.7%**) |
| ASML-US | 796.1(−51.8%) | 1131.9(−31.5%) |
| AMD-US | 180.0(−62.9%) | 204.4(−57.9%) |

前两家的结论**翻了个面**:从"低估、可以看多"变成"高估、该减"。后两家幅度虽然没翻号,
但一个把 −32% 说成 −52%,也足够让人做出完全不同的仓位决定。

这类 bug 最坏的地方不是错,是**它不报错**:不抛异常、不写日志、不留红字,
只是安静地给出一个反的结论。一个会崩溃的 bug 当天就被修了,这种能活几个月。

### 改法一:认数据自己带的那行标签

FactSet 在 `Estimate History` 那一行的右边写了财年末:`FactSet Estimate History Jan '27E | 2026-07-30 01:21Z`。
现在拿它和这批数据**最新一行的日期**比,差满几个整年就是第几个财年;文件名只在读不到标签时兜底。

实测:NVDA 三个文件的月差分别是 6 / 18 / 30 → FY1 / FY2 / FY3;GOOGL 是 5 / 17 / 29 → 同样对上。
已经过完但还没出报的财年月差会 ≤0,下限钳在 1,不让它变成 0 或负数。

为什么非要绕开文件名:文件名是**我们自己**在 `fetcher/lib/browser.mjs` 里贴上去的标签,
它记录的是"抓取时以为自己在哪个页签",而不是"表里到底是哪个财年"。
拿一个可能出错的判断去校验另一个出错的结果,等于没校验。表里那行标签是 FactSet 写的,是数据的一部分。

### 改法二:第三财年及以后,只借价格序列

判定对了之后还要决定收什么。现在的规矩是:

- **EPS**:FY1 / FY2 各进各的槽,FY≥3 一概不收 —— 三年后的盈利乘不上明年的估值分位。
- **P/E 分位库**:只认 FY1。FY2 也不收 —— 口径统一以 FY1/NTM 为准,混进来一半 FY2 的点,那条分位曲线就谁也代表不了。
- **修正动量**(上调/下调家数):只认 FY1,那是方向信号,得和估值口径同源。
- **价格序列**(`P/E × Mean` 逐月还原):**照收**。还原出来的是同一支股票那天的股价,与用哪个财年的口径无关 ——
  这条对波动率统计有用,没有理由扔掉。

被跳过的时候卡片上会写清楚是第几个财年、跳过了什么,不静默。

### 改法三:一条通用护栏,防下一次

上面两条修的是这一次。护栏管的是下一次 —— 因为让人栽跟头的从来不是已知的那个错法。

`P/E 分位库的最新一点 × 基准 EPS` 本该约等于现价:这两个数原本就摆在同一份 Estimate History 的同一行上,
乘回去就是那一行当天的股价。偏离超过 25%,卡片上直接出红字。

25% 这个阈值是量出来的,不是拍的:八份配对正确的真实文件,偏差落在 **−11.5% ~ +0.9%**
(价格快照和估值快照差几天,加上 P/E 只有一位小数);FY3 配 FY1 那次是 **−40%**。中间空得很宽。

**它只报警,不改数。** 说不清是分位错了还是 EPS 错了的时候,不该替人做主 ——
自动"修正"到一个看起来合理的数,比显示一个明显不合理的数更危险。
红字也一定要落在卡片上,不能只写 console:护栏只在控制台响,等于没响。

### 顺带查清的两件事:都不是 bug

- **AMD 中枢 −58%**:真实读数。现价 485.39 除以 FY1 EPS 7.50 是 57.3 倍,而它自己近两年的 P/E 中位数是 27.3 倍。
  卡片说的是"按你这两年给它的估值,现价贵了一倍多",这句话没错。
- **SPCX 卡片空白**:正确行为。FY1 EPS 是 −0.35,乘数法在亏损时无定义,`calcRange` 返回 null 是对的。

另外注意到一个纯外观问题(未改):卡片标题写成「NVDA-US NVDA-US」,因为 `companies.csv` 里 `name` 字段填的就是代码本身。

### 测试

`tests/test-app.mjs` 新增第 [12] 组,12 条断言,116 → **128**。全部用 FactSet 形状的 `aoa` 夹具驱动真实的
`ingestEstimateSheet` / `calcRange` / `renderAll`,不 mock 中间层。其中两条专钉加载顺序:
「FY1 先进、FY3 后进」和「FY3 先进、FY1 后进」——**后者正是当初翻车的那个顺序**,
只测一个方向的话,这个 bug 能原样躺回来。

还有一条钉的是"红字真的出现在 DOM 里"而不只是对象上有 `baseGap` 字段。

### 那五个文件

`Assets/estimates/` 下的 `AMD-US` / `ASML-US` / `GOOGL-US` / `NVDA-US` / `SPCX-US` 的 `FY3 Estimate History.xlsx`
已挪进 `_to_delete/`,没有删。修完之后它们已经不会污染任何数字(FY≥3 只借价格序列),
挪走只是免得下次翻文件夹时又疑惑一次。最后那一下删除还是你按。

## v17.0 (2026-07-30) —— 仪表盘只画拉取清单里的公司;落榜的数据留满一年再挪走

你问的是这个:「要么仪表台上先不显示 我清单上没有的?这样数据也不删,除非满了我们之前达到的一年。」
下面就是按这句话做的,一字不改地拆成两件事。

### 1. 显示归显示:清单外的公司默认不画

老毛病很具体 —— **仪表盘扫的是文件夹,不是清单**。你从 `fetcher/tickers.txt` 里删掉一个代码,
数据还在 `Assets/` 里躺着,于是它照样出现在表格里,只是从此再也不更新。
那一行看不出任何异常,你却会照着一份停在几个月前的价格做判断。
比"数据不见了"危险得多:少了你会去查,旧了你只会照着做决定。

现在 fetcher 每次启动往 `Assets/summary/roster.csv` 写一份当前清单(公司 + 市场级序列),
仪表盘读它来决定画谁。表格下面留一行「另有 N 家不在拉取清单里,已隐藏」,点一下展开,再点收起。

三条边界写在代码里也钉在测试里,因为它们失效的时候**终端上一个字都不会报**:

- **没有 roster.csv 就完全不过滤。** 旧的 Assets 文件夹、演示数据、别人发来的一堆表,行为和以前一模一样。
  "没有清单"和"清单是空的"是两件事,混了就会有人打开 app 看见一张空表格。
- **清单和数据完全对不上时也不过滤。** 宁可多显示,也不要静默地什么都不显示 —— 空表格看起来就和 app 坏了一样。
- **那个展开开关不参与任何计算。** 走向概率的"默认同行"改成只取清单内的公司(落榜的那些数据可能已经停了几个月,
  拿它们的修正动量去平均,等于让一批过期样本悄悄参与定价),但它看的是清单,不是开关。
  点一下"展开看看"如果连同行中位数一起变了,同一家公司的估值区间会因为你点了个提示行而漂移。

顺带修掉一个连带问题:选中的公司被隐藏时,详情卡片会指着一家表格里根本没有的行。现在会自动改选画得出来的那家。

`roster.csv` 末尾有一行以 `#` 开头的说明。CSV 没有注释语法,所以那行会被解析成一行数据 ——
仪表盘靠"ticker 以 `#` 开头就跳过"把它滤掉。这是两边对好的一对约定,自检两侧各钉了一条:
改了措辞不要紧,改掉那个 `#` 就会凭空多出一家叫「这份文件…」的公司。

### 2. 清理归清理:落榜满一年才动,而且是挪不是删

`fetcher/lib/roster.mjs` 每次启动扫一遍 `Assets/`,不在清单里、**且最后一次更新已过一年**的,
整组挪进仓库根的 `_to_delete/`,并当场打出挪了谁、几个文件、最后更新是哪天。
最后那一下删除由你按 —— 脚本不替你销毁攒了一年的授权数据。

一年这个数字不是随手定的:和期权链滚存用的是同一个界。两处口径不一样,以后没人说得清"我的历史到底留多久"。

判定用文件 mtime,不另存一个"删除日期":清单一删,那个代码就不再被抓,它所有文件的 mtime 就此冻结在最后一次抓取那天。
mtime 本身就是"它凉了多久"。少一份状态,就少一次两边对不上的机会。

四条护栏,同样是"不报错但后果很大"那一类:

- **清单为空时一个都不动。** 空清单多半是文件被清掉或读失败,而不是"这些我全都不要了"。
  按字面办事会把整个 `Assets/` 一次性搬空。
- **按代码成组判定,不按单个文件。** 只要这个代码还有任何一个文件比一年新,整组都留下。
  逐个挪的话,一年半的日线被挪走、半年前的估值还留着,剩下的是一份自相矛盾的历史 ——
  仪表盘照读不误,画出来的东西却谁也解释不了。
- **市场级序列(`SPY-US` 等)算进"还在用"的集合。** 它们写在 `markets.txt` 而不是 `tickers.txt`,
  漏掉的话 SPY 的日线一年后会被当成落榜公司挪走,而走向概率的基准轨会安静地空掉。
- **读不到修改时间就别猜,留着。**

写清单这一步排在"清单与数据对齐"之后,顺序不能反:对齐那一步会把"有数据没登记"的代码补进清单,
先写就会写出一份少了它们的清单,仪表盘据此把刚补进来的公司藏起来一整轮。

### 顺带

`retire()`(挪进 `_to_delete/`、重名加序号、绝不真删)从 `migrate.mjs` 导出了。
"挪不删"这条规矩只该有一个实现 —— 另写一份迟早会有一份忘了加序号,然后同名文件互相盖掉。

自检 220 → **233**,仪表盘测试 104 → **116**。

## v16.9 (2026-07-30) —— 修 v16.8 的两处:回测在真机上根本没跑起来;FY2 失败时台账写着 OK

两条都是"报告说没事、其实有事"的毛病,所以放一起。

### 1. 每月回测在你的机器上一次都没跑成

v16.8 上机第一轮就崩了:

```
每月回测  还没跑过,这个月该跑了 —— 只出报告,不改任何权重。
Error [ERR_MODULE_NOT_FOUND]: Cannot find package 'xlsx' imported from
  ...\Price Range Dashboard\tools\backtest.mjs
回测没跑成(exit 1),不影响这轮抓取的数据。
```

原因是我在 `tools/backtest.mjs` 里写了 `import * as XLSX from 'xlsx'`,
并且在 `fetcher/lib/backtest.mjs` 里给它配了一句**错的**注释,说 `cwd` 设成仓库根就能解析到依赖。

**ESM 的裸模块名是从引用它的那个文件所在目录往上找 `node_modules` 的,跟 `cwd` 一点关系都没有。**
这个脚本在 `tools/`,只会看 `tools/node_modules` 和 `根/node_modules`;
而 `run-factset.bat` 首次启动是在 `fetcher/` 里 `npm i` 的,包全躺在 `fetcher/node_modules`。
于是:我这边(手动在根目录装过)三道闸门全绿,你那边必崩——**而你那台才是它平时运行的地方**。

改法:

- `tools/backtest.mjs` 显式按三个锚点 `createRequire`:本模块 → `fetcher/` → 仓库根。
  三处都没有就自己打一句人话(连同找过的三个目录)并 `exit 3`,不让 Node 把堆栈糊到终端上。
- `fetcher/lib/backtest.mjs` 认 `3` 这个码,翻成「在 fetcher/ 里跑一次 `npm i xlsx`」;
  其它非零码**不转述**——真错误子进程已经原样打出来了,再复述一遍是噪音。
- 那句错注释删掉,原地换成"cwd 不影响依赖解析"的正确说法。留着比没有更害人。
- 自检加两条钉死:静态裸导入 `'xlsx'` 不许回来;退出码 3 的约定两边要对得上
  (这个约定要是哪天错开,表现是"装依赖的提示不出现",终端上看不出任何异常)。

验证方式不是"我读一遍代码觉得对":把根目录 `node_modules` 藏起来、
只在 `fetcher/node_modules` 放一份 `xlsx`,复现你机器的目录布局,再跑一遍——这次跑通了。

### 2. `TSM-US ✖FY2`:失败了,但台账里那行写着 OK

同一轮里 TSM 的 FY2 打了 ✖,提示「详见 sources.txt」,而 sources.txt 里那行是
`OK | TSM-US · 估值历史 FY2 | ... | 2026-07-29`——上一轮留下的。日志里连一行 FY2 都没有。

翻代码找到了:那一轮页面上的财年标签(`Jul '26E` 那一格)没读出来,`p0` 是 `null`,
而"切到另一个财年"整段是 `if (p0 && …)`。标签一空,FY2 不是失败,是**根本没被尝试**——
没进 `step()`,自然不写台账,于是汇总表打 ✖、台账写 OK,还让你去查那份说"一切正常"的文件。

- `currentPeriod()` 改成**重试 3 次**(每次隔 1.2 秒)。表格本体和这一格是分开渲染的,
  表格出来了不代表标签出来了——这多半只是抢跑。
- 重试完还是空,就走 `step()` **显式抛错**,台账写 FAIL、失败环节点名「解析行」。
  拿不到就是拿不到,但不能让台账替它说没事。
- 顺带补一句提醒:标签为空时那份表是**按 FY1 猜着存**的(FactSet 默认页就是 FY1,
  绝大多数时候猜得对)。猜错就是拿 FY2 的表盖掉 FY1 的文件,而文件名看不出任何异常,
  所以现在会明说一句,并告诉你怎么删了重拉。

自检 215 → **220** 条。`npm test` 104 条不变。

### 顺带回答:`QQQ-US` 为什么在仪表盘上,却不在拉取清单里

查了你机器上的 `tickers.txt`,不是 bug,是你自己删的,而且删对了:

```
# ignore: QQQ-US
# ignore: SPY-US
```

这两行是 v16.x 那条「删清单不删数据」的规矩留下的——你删掉的标的不会被 `Assets/` 里的
存量数据复活。但仪表盘扫的是**文件夹**不是清单,`Assets/estimates/QQQ-US FY2 Estimate History.xlsx`
还在,所以它照样显示,只是数据永远停在被删那天,而且只有 FY2 没有 FY1——
这正是它显示「缺少估值分位或 EPS ≤ 0」的原因(`SPY-US` 同理)。

想让它彻底消失:把 `Assets/` 下那几个 `QQQ-US *` / `SPY-US *` 文件挪走。
想让它活过来:删掉对应的 `# ignore:` 行。两条路都得你自己选,脚本不替你决定。
(`SPY-US` 作为市场基准另有一份 `_MARKET-BENCH SPY-US Daily Charting.xlsx`,那份是在正常更新的,
别一起挪走了。)

## v16.8 (2026-07-30) —— 期权链不再覆盖写;回测多一组、留台账、每月自己跑

三件事:**存下来**(期权链按日期追加,不再一天天盖掉)、**多测一组**(新闻情绪,
上一版被我错误地写成"没历史"的那条腿)、**记下来**(每轮回测的关键数字进台账,每月自动跑一次)。

### 1. `Options.csv` 改成按 `asof` 追加,保留一年

旧写法里藏着两条杀历史的语句,一条都不能留:

```js
seen.set(c[1] + '|' + c[2], row);      // 键少了 asof → 今天的快照原地盖掉昨天那行
rows.filter(r => r.expiry >= today);   // 到期的链整段删掉
```

而"某个行权价上那堵 OI 墙,后来到底顶住没有"——要回测这句话,要的恰好就是
**已经到期的、按日期一层层叠起来的**那些行。

合并与滚存拎成了纯函数 `fetcher/lib/opt-store.mjs`(旧代码混着 `fs`,自检只能干看着):

- 键改成 `asof|到期日|行权价`。**同一天重跑就地覆盖,跨天各存一层**——这就是"不覆盖"的全部含义。
- 超过 **365 天**的整段快照滚掉。实测一轮约 37 行,天天跑一年约 9.3k 行(≈350 KB),全书约 3 MB。
- 行数上限 20000(两年余量)。超限时**按整份最老的快照往外扔,不是从尾巴切一刀**——
  切一刀会留下半份链,而半份链算出来的 max pain 是错的,比没有更糟。**今天这一份永远不动**。
- `asof` 或到期日不是合法日期的行直接丢弃,并**报出丢了几行**,不闷声删。

**仪表盘一行都没改**,而且是查过代码才敢这么说的:`src/js/ingest/signals.js` 的
`ingestOptions` 本来就按 `到期日|行权价` 去重并**取 asof 最新的那条**;
`src/js/pressure/options.js:57` 本来就 `if (r.expiry <= today) continue;`。
历史行进不了当期读数,到期链也污染不了 OI 墙 / max pain 面板。

归档只动期权链一处。新闻本来就累积,`short-interest.csv` 本来就按日期追加,
charting / estimates / targets 三份 xlsx 各自内部带着历史——**它们该被覆盖,覆盖才是对的**。
另外没有做"按日期建子目录":`src/js/ingest/folder.js` 的 `collectDirFiles` 往下钻三层
且**不看目录名**,任何日期子目录都会被整个吞进去重复导入。追加进同一个文件,没有这个问题。

### 2. 回测补上 F 组:新闻情绪(并更正 v16.7 的一处错误)

v16.7 的报告和 CHANGELOG 都写着"新闻情绪只有单点,没有历史可回放"。**这是错的。**
`Assets/news/` 是 fetcher 逐轮攒出来的,盘上有一年、7 只票、**1416 条**带日期的标题。
是我没看数据就下的结论,白扔了一整组。

这条腿在构造上偷不了看:`newsScore(ticker, 参照日)` 的第二个参数就是"站在哪一天看",
未来的标题被它自己的 `年龄 < 0 就跳过` 挡在外面。回测走的也是仪表盘的 `ingestNews` 本尊,
去重和日期校验都不另写一遍。

有效样本按 **`max(前瞻, 30)`** 折算,不是按前瞻:打分窗口本身有 30 天记忆,
5 日前瞻下相邻两天读到的是同一批标题,按 `n/5` 算会把独立样本高估六倍。

结果:5 日 / 21 日**看不出**(rho 0.01 / −0.05),63 日样本太少。诚实的空结果。

### 3. `--log` 台账 + `bt` 命令 + 每月自动跑一轮

- `node tools/backtest.mjs --log` 把这轮 34 个关键数字追加进 `Assets/_logs/backtest-history.csv`。
  存的是**长表**(一行一个指标),不是宽表——以后加一条腿只是多几行,宽表得改表头,
  几个月后的历史就对不上列了。同一天重跑替换当天的行,不叠两份。
- `verdict` 一列是**受控词表**(`holds` / `inverted` / `inconclusive` / `too_narrow` /
  `too_wide` / `trending` / `mean_reverting` / `no_variance` / `biased` / `recorded`)。
  加词可以,改词不行——改了,几个月前那些行就跟今天的行对不上了。
  其中 `recorded` 是"没有基准,只记数不判对错":没有基准还盖个绿灯,是自己给自己发绿灯。
- fetcher 菜单加 `bt`,随时手动跑一轮。**手动跑也照样记账**——只在结果好看时才记的账,
  过几个月就是一份专门骗自己的历史。
- 抓完一轮后,若本自然月还没跑过就自动跑一轮(一次启动最多触发一回)。
  按自然月比而不是"距上次满 30 天":后者会让跑批日期每月往后漂,漂上半年就分不清轮次了。

**回测不会自动改任何权重,一个数都不会动。** 这不是谨慎,是算术:一年日线只有约 32 个
不重叠的 21 日窗口,看见红灯就调参,调的是这 32 个窗口的噪声。报告可以自动出,手必须是人的。
真正值得回头翻这份台账的时刻只有一个——**某条腿的判语从"看不出"翻成了"反了"**。

期权轨这一版仍然测不了:链的历史从现在才开始攒,大约半年后够用。

自检 208 → **215** 条(滚存边界 + 排期共 25 条)。`npm test` 104 条不变。

---

## v16.7 (2026-07-30) —— 回测:这几条线过去到底准不准

`npm test` 一直只回答"算得对不对"。这一版加 `tools/backtest.mjs`,回答另一个问题:
**画出来的区间和概率,拿过去的行情对一遍,站不站得住。**

### 测的是仪表盘真在跑的那份代码

用 `node:vm` 把 `src/js/**` 按 manifest 顺序原样装进沙箱,调 `volStats` / `priceDensity` /
`peStats` / `dirScores` 本尊。照着重写一份来测,测的是我理解得对不对,不是它算得对不对——那种绿灯没有意义。

装的时候踩到一个小坑:`const state = {...}` 是词法声明,**不会挂到全局对象上**,
沙箱外面拿不到(函数声明会)。多跑一行 `globalThis.state = state` 搭桥,并加一条断言钉住——
`state` 结构再变,坏的是断言,不是半个月后的结论。

### 第一版报了四个红,四个都是假的

滚动窗口的样本是**重叠的**:h=63 时相邻两个观测共用 62 天走势,它们不是两条独立证据。
第一版的命中率和 z 都按 n 算,于是:

```
                第一版        折算 effN=n/h 之后
63日 ±1σ        z=-9.4   →   z=-1.2
21日 ±2σ        z=-9.9   →   z=-2.2
综合倾斜         z 判"反了" →  看不出
技术位 vs 随机带  z=-3.8   →   z=-1.9(且有效样本只有 22 轮)
```

**一个纯噪声的结果被打扮成 z=-9,而它其实是 z=-1.2。** 命中率照 n 算(那是最好的点估计),
但标准误只能按有效样本算。现在 `zBinom()` 的 `effN` 是**必传参数**,每张表都有"独立"这一列。

反方向的自欺一样要防:`effN` 小的时候也不许判"合格"。63 日 ±1σ 实测 49.6%(应 68.3%),
判语是"样本太少,看不出"——**没测出问题,不等于没问题。**

### 三条真结论

- **一年 ±1σ 区间在数据上无法验证**:盘上只有一年日线,一个前瞻样本都没有。不假装能测,
  改测它脚下的两个假设(短周期滚动 σ 的校准、√t 外推的方差比)。5 日校准合格;
  21 日 ±2σ 只覆盖 88.5%(应 95.4),**尾部偏厚,区间在极端那一侧偏窄**;
  63 日方差比 0.54,均值回复,按 √t 外推到一年会画宽。
- **估值中枢显著输给"价格不动"**(3 月 MAE 之比 1.92,独立样本 72)。而且不是误差大,
  是**单边偏低**:中枢相对现价中位 **−18.2%**,84% 的时点挂在现价下方。
  P/E 分位取自这只票自己的历史,这一年又是再评级行情,历史中位 P/E 一直追不上当前 P/E。
  区间还能当"贵没贵"的参照,**但别拿中枢当目标价**。
- **走向面板的"行业"腿整段历史恒为 1.00**,没产生过任何区分度。常数不是信号,
  原来它还能在表里冒充一条腿,现在会被单独点名。

其余各条一律"看不出"。**这是诚实的结果,不是失败的结果**:一年日线撑不起统计检验,
需要的是更长的历史,不是更花的指标。

### 期权轨一条都没测

它一个历史快照都没有——链和空头持仓都是单点。
fetcher 每轮把 `Options.csv` 按日期归档即可,一个月后这一组就能补上。

> **v16.8 更正**:上面这句原本还写着"新闻情绪也是单点"。**那是错的。**
> `Assets/news/` 是 fetcher 逐轮累积的,盘上就有一年、七只票、约 1400 条带日期的标题,
> 而 `newsScore(ticker, 参照日)` 本来就收参照日。这条腿一直是能回测的,
> 是我没看数据就下了结论,白扔了一整组。v16.8 已补上(F 组)。

回测读 `Assets/`(授权数据、不进仓库),所以**不进 `npm test`**,单独 `npm run backtest`。

## v16.6 (2026-07-30) —— 期权链改走接口:不用手动导出,也不用刮屏

上一版把期权那一步从刮屏改成了"你手动导出一次,脚本自己去收"——读得全了,但多了一双手。
这一版把那双手也去掉了:**FactSet 前端自己用的两个接口,脚本可以直接调**。

### 数据源不是那张表格,是表格背后的两个接口

顺着 Options Montage 的应用包(`options-montage/dynamic/main.*.bundle.js`)搜 `services/`,
两个端点直接写在 `getRequestParams` 里。都在 `my.apps.factset.com` 同源下,
带上 `credentials:'include'` 用现成的登录态就能调,不需要 token、不需要点任何按钮:

- `GET /services/IDCServ/oc?symbol=<代码>` —— **期权链目录**。竖线分隔的纯文本,25 列,
  第 0 行是表头。**每一行结尾都多一根竖线**,所以解析要 `.split('|').slice(0,-1)`,
  不然凭空多出一列空值。里面有根符号、两条腿的实时代码、行权价、到期日、
  合约乘数、是否调整过、标的代码——**唯独没有未平仓量**,它是一份目录,不是数据
- `POST /services/Fql?app=html_reports&string_na=1`,表单体 `symbols=<逗号分隔>&exprs=<表达式>`
  —— 按合约代码取值。返回扁平 JSON 数组,每项 `{$error,$expression,$symbol,$value:[[数]]}`。
  可用标识符实测有 `P_OPT_OPEN_INTEREST` / `P_OPT_STRIKE_PRICE` / `P_OPT_EXP_DATE` /
  `P_OPT_VOLUME` / `P_OPT_DELTA` / `P_PRICE`

**实测吞吐(2026-07-30,NVDA):** 目录 1947 行 / 471 KB,一路排到 2028 年 12 月;
筛出 60 天内、离钱 ±35% 的合约后 **626 条腿一个请求 2805 毫秒全部返回,零报错**,
未平仓量合计 331 万。**导出那条路一次最多 500 个合约的硬上限,在这里不存在了。**

### 这条路自己的坑:200 不等于拿到了

代码不认识、或者这只标的压根没有期权(实测 `ZZZZQQ-USA`、`0700-HK`),
接口回的是 **HTTP 200 加一个空 body**,不是 404。
`parseOptionChainTable('')` 因此明确判失败并说明理由,不许它变成"成功但零行"——
那正是空头持仓和虚拟滚动那两次栽的同一个跟头:**请求成功不等于拿到了链**。

### 三层降级,顺序按"读得全不全"排

`接口` → `导出收件箱` → `刮屏`。接口那层失败时**先把原因原样打进日志再往下走**,
不静默降级;刮屏那层的 ⚠ 原样保留。三层都产出同一种行(`到期日/行权价/看涨OI/看跌OI`),
下游 `saveOptionsCsv` 和压力位面板一个字都不用改。

### 新文件 `fetcher/lib/fql.mjs`——只放纯函数

解析、筛选、切批、拼装、验收全在这里,一行网络代码都没有;
`steps/options.mjs` 只负责"把字节拿回来"。这样**解析错了自检会红,网络错了台账会红**,
不用靠猜。筛合约那一步的每种丢弃理由都单独计数(过期 / 太远 / 离钱太远 / 调整过 / 标的对不上),
日志里看得见丢了什么,而不是只看见剩了多少。

验收线在 `optApiVerdict`:没取到未平仓量的腿超过 **20%** 就整份拒收——
缺一块的链算出来的 max pain 和 OI 墙是错的,**宁可这一轮不写**。

### 自检 +36 项(154 → 190)

固件是 **2026-07-30 从接口原样抄回来的字节**:真实的 25 列表头、
NVDA 三个真实到期月的目录行、真实的未平仓量数字(8662 / 33852 / 18334 / 48130 / 14390)。
只有两行标注为"改出来的"(把 `ADJUSTED` 翻成 `Y`、把标的翻成 AMD),因为 NVDA 眼下没有调整过的合约。
**断言写得像返回的字节,而不是像我想象中的返回,才挡得住下一次接口改字段。**

## v16.5 (2026-07-30) —— 修:空头持仓读的是隔壁那一格 + 期权链从来就没读全过

上一版给空头持仓加了量级自洽护栏,拦住了错的数字进面板;这一版补的是**根因**。
顺着同一条思路回头查期权,发现那一步有个更安静的同类问题:它从来没失败过,也从来没读全过。

### 空头持仓:根因不在正则,在取数的范围

- Ownership 面板上三个统计块是并排的,整个 frame 的 `innerText` 把它们拼成一段:
  `SHORT INTEREST 2.5 DAYS / 1.4% FLOAT  FLOAT 96.2%  INST. OWNERSHIP 72.1% OF FLOAT`。
  `%\s*of\s*Float` 于是越过空头那一格,一路匹配到机构持股的 72.1。
  **正则没写错,它只是被喂了不该看见的文本**——再补多少条更精细的正则都是在补错的地方
- 新增 `lib/browser.mjs > frameBlockContaining(label)`:找到写着标签的叶子元素,
  **往上爬到"最小的、文本里含数字的祖先"**,只交出那一格。
  隔壁的数字从此在**结构上**够不着:排版可以改,"数字和它的标题在同一个盒子里"不会改
- 退回整页文本时先用 `siBlockTooWide()` 数百分号,一段里超过一个 `%` 就判为范围过宽,
  **宁可失败也不猜**。真实页面还证明了另一件事:那行字写的是 `1.4% FLOAT`,大写、**没有 `of`**——
  旧断言全都带着 `of`,所以它们一直是绿的,而线上七只标的全读错了
- 自检里这批断言改成**照抄实地原文**,外加一条反向断言把三块拼在一起,钉住"必须读回 1.4 而不是 72.1"。
  **断言写得像页面而不像正则,才能挡住下一次**

### 期权:改走导出。刮屏在这一页上结构性地读不全

- Options Montage 的表格是**虚拟滚动**的,DOM 里永远只有视口内那二十来行。
  少读**不会失败**——它安安静静写出一份看着挺正常的 `Options.csv`,
  然后压力位面板拿着这份残缺的链一本正经地算 max pain 和 OI 墙。
  和空头持仓那次是同一类错:**读到了不等于读全了**
- 主路改成读 FactSet 的 xlsx 导出:按顺序扫
  `%FS_OPT_INBOX%` → `fetcher/options-inbox/` → `~/Downloads` 里的 `optionsMontage_*.xlsx`,
  不用改名不用整理。**sheet 名(`NVDA-USA_20260728`)就是这份文件的身份证**——
  认标的、取快照日期(而不是拿"今天"冒充)、对不上**整份拒收**;
  逐行再核一次 `Root Symbol`(调整过的合约带后缀,所以是前缀匹配)。
  下载目录里什么都有,**混一条别人家的链进去比没有数据危险得多**
- 导出一次最多 **500 个合约**,超了**一个文件都不给**(不是给你截断的那种),
  所以按月份分几次下是常态:同一个"到期日+行权价"以 `asof` 最新的那份为准,几份自动拼成一条链。
  导出时那个 **% from At-the-Money = 25** 既是为了压进 500 上限,也正好等于仪表盘的 `OPT_WINDOW`
- 到期日**精确到天**,不再走"某月第三个周五"的推算——周度期权早就把这个假设废了
- 刮屏保留为兜底,但成功时也会打一条显眼的 ⚠ 说明这一轮的链很可能不全,并把导出的点法整份打出来
- 实测:NVDA 一份导出 48 个行权价 / 12 个到期日;SPCX 两份月度导出合并成 355 个行权价 / 5 个到期日

### 仪表盘:导出到位以后,期权轨的三个参数全都站不住了

`src/js/pressure/options.js` 原来那组参数是在"每轮只刮到二十来行、到期日只有月度粒度"的前提下定的。
前提一换,**参数不动的话面板会一本正经地算错**:

- **周度期权把月度挤出去了。** NVDA 一份导出里就有 12 个到期日,"最近两个"选中的是
  08/03、08/05 这种几乎没人参与的薄周度链,而 OI 真正堆着的月度 08/21 被整个忽略。
  选到期日的标准改成"**视野内、窗口里 OI 最重的三个**"(`OPT_EXPIRIES` 2 → 3,`OPT_MAX_DTE = 60`),
  时间远近改由 `w = 1/(1+dte/30)` 连续衰减表达,不再是写死的 `[1, 0.6]`
- **零 OI 行把均值压下去了。** 导出给的是整条链,一大半行权价 OI 是 0;
  分母一涨,`avg × 1.5` 这道门槛形同虚设,于是满屏都是"墙"。均值只在**真的有人参与的行权价**上算
- **当天到期的链占着最高权重。** 原来是 `expiry < today` 才丢,于是今晚就归零的那条链
  还拿走权重最高的第一个名额。改成 `<=`
- 自检 119 → **154 项**,仪表盘断言 99 → **104 项**(新增:导出解析与合并、
  空头持仓实地原文与反向串格、按 OI 选到期日、当日到期不占名额、零 OI 不进分母)

## v16.4 (2026-07-29) —— 修:空头持仓那一步"改版了"却说不出改成什么样

`⚠ AMD-US · 空头持仓 失败 —— 断在【解析行 :: 未找到 Short Interest 字段(Ownership 页面板改版)】`
这句报错的问题不在它错了,在它**什么都没说**:页面上到底有没有那个面板、有的话写的是什么,
一个字都不告诉你。于是每次坏掉都得人肉开一次浏览器复现,而这一步一坏就是好几周。

- **等到出现为止,不再赌固定 8 秒。** 原来是 `waitForTimeout(8000)` 之后读一次,
  读不到就判死。FactSet 的 Ownership 面板是异步渲染的,慢一点就成了"改版";
  现在改成最多轮询 45 秒,**判据是"解析成功"而不是"文字出现"**——
  面板文字先出、数字后填是常态,只等文字会稳定地拿到一个空面板
- **两个数字分开找。** 原来一条正则要求"回补天数 / 占流通盘%"必须以固定顺序、
  固定连接符出现在同一段文本里;表格换成两行两列就整步失败,连**已经读到的那个也一起扔掉**。
  现在快路径仍走老正则,不中则两个数字各找各的,只读到一个就先记一个
  (缺的那格在 csv 里**留空而不是写 NaN**,仪表盘本来就按空值跳过)
- **失败时把页面原文摘三段打进日志。** 这条比前两条都值钱:
  下一次真改版时,日志里直接有证据可以照着改正则,不用再复现一遍。
  同时区分开两种失败——"面板压根没出现"(多半是没登录/这只标的没这项数据)
  与"面板在但读不出数字"(才是真改版),它们的下一步动作完全不同
- 新增通用原语 `lib/browser.mjs > frameTextContaining()`:交出命中 frame 的**整段**可见文本。
  `findTextInFrames()` 只返回命中的那一小段,这正是"除了'改版了'什么都说不出"的根源
- 数量护栏:回补天数 0–200 天、占流通盘 0–100%,越界视为读错了格子,不写盘
- **量级自洽护栏 `shortIntSanity()`——这次修的其实是一个比"失败"严重得多的问题。**
  查台账时发现 2026-07-29 那一轮**并不是全都失败**:15:42 之后七只标的全部"成功",
  写进 `short-interest.csv` 的却是 AMD 68.7%、GOOGL 80.7%、MSFT 76%、NVDA 72.1% 占流通盘——
  这是机构持股那一格的数字。**失败会报警,错的数字不会**:它一路走进走向概率面板,
  让每只大盘股常年吃一记 −0.25 的空头扣分(权重 0.25),屏幕上看不出任何异样
  - 拆穿它不用看页面,两个数字自己就互相矛盾:
    `占流通盘% ÷ 回补天数 =(空头/流通盘)÷(空头/日均量)= 日均成交量占流通盘的百分比`。
    AMD 是 68.7 ÷ 1.6 ≈ **每天换手 43% 的流通盘**,NVDA ≈ 29%,都高出一个数量级
  - 所以卡的是这个**比值**而不是拍脑袋的上限:`SI_MAX_PCT = 50`、`SI_MAX_TURNOVER = 20`,
    就写在 `steps/short-interest.mjs` 顶部。不自洽就当没读到,连同页面原文一起判失败——
    **宁可这一格空着,也不能拿错的数去打分**
- 自检 105 → 119 项(拆表格、只有占比、量纲越界、量级自洽、失败诊断各有断言)

## v16.3 (2026-07-29) —— 修:bat 双击报错 + 清单与数据对不上

- **`run-factset.bat` 双击直接报错,重写。** 两个原因叠在一起:
  文件是 **LF 结尾**的(cmd.exe 对 LF-only 容忍度极低,里面四个多行 `if ... (` 块必坏,
  报的还是"命令语法不正确"这种指不到位置的错);文件里又同时有**中文和 `chcp 65001`**——
  cmd 用**读到那一行时**生效的代码页去解码,文件中途切代码页救不了前面已经读过的行
  - 处理:纯 ASCII + CRLF + 用 `goto` 标签代替多行括号块,每条错误路径都 `pause` 住;
    所有中文提示交给 Node 脚本输出,那边没有这个问题
  - 新增 **`.gitattributes`** 把 `*.bat` / `*.cmd` 钉成 `eol=crlf`,谁在哪个平台 checkout 都一样。
    这类 bug 修一次不算修完,得让它不可能再回来
- **新增"清单与数据对齐"(`lib/reconcile.mjs`),每次启动自动跑。**
  你的 `Assets/` 里有 6 家公司的数据,`tickers.txt` 只登记了 2 家——另外 4 家每轮都被静默跳过。
  而仪表盘扫的是**文件夹**不是清单,所以它们照常显示,只是数据停在几周前。
  **缺数据你会去查,旧数据你只会照着做判断**,后者危险得多
  - "有数据却没登记"的**直接补进 `tickers.txt`**,并当场报出补了谁、有哪几类、缺哪几类
    (这次补进了 AMD-US / ASML-US / MSFT-US / SPCX-US,四家都缺期权)
  - "登记了却一个文件都没有""数据不全"只报告,不动清单——那可能是刚加进去还没拉,
    也可能是拉失败了,哪种都不该由脚本替你决定
  - 判断只看**文件名**,与 `registry.mjs` 的产出命名、仪表盘 `ingest/` 的识别规则同一套口径
  - 菜单新增 `sync` 可随时手动再对一次
- **删除不再被"复活":`tickers.txt` 里的 `# ignore: 代码` 注释行。**
  删清单并不删数据,所以上一条如果没有例外机制,你今天删掉 SPCX-US,明天启动它就自己长回来了。
  一个东西被删掉之后又出现,比工具少干点活严重得多。想恢复:删掉那行 ignore,或把代码再输一遍
- **自检不再往真实 `Assets/` 里写东西(`lib/selftest-env.mjs`)。**
  `--selftest` 里的 `saveEstimateXlsx('TEST-US', ...)` 和三个 `_selftest *.xlsx` 一直走
  `assetPath()` 落进真实目录,于是 `estimates/` 里长出一个从没人拉过的 `TEST-US`。
  新加的对齐检查一扫,理所当然地把它当成"你拉过但忘了登记的标的"——
  **一个自检污染了它本该验证的东西,再由另一个检查如实报告出来,每一步都没错,合起来是错的**
  - 现在 `--selftest` 的输出改到系统临时目录。这件事必须做成入口的**第一个 import**:
    `OUT_DIR` 是模块顶层 const,而 ESM 的 import 全部先于模块体执行,在函数里改环境变量已经太晚
  - 已经落在盘上的 5 个遗留文件,由 `migrate.mjs` 启动时挪进 `_to_delete/`(不删,自己确认后清空)
- 自检 80 → 105 项

## v16.2 (2026-07-29) —— 修:数据写去了没人看的地方 + Assets 分层

上一版看着"每一步都成功",仪表盘却一个新数据也没有。原因不在抓取,在**落盘位置**。

- **根因:`import.meta.url` 的锚点跟着文件走。** v16 把 `config.mjs` 从 `fetcher/`
  挪进 `fetcher/lib/`,那个叫 `SCRIPT_DIR` 的锚点就悄悄深了一层,于是数据进了
  `fetcher\Assets\`、台账进了 `fetcher\lib\`。全程没有一行报错,因为每一步单看都是对的
  - 处理:**删掉 `SCRIPT_DIR` 这个名字**(它不说明自己是哪一层,谁用谁错一层),
    换成三个说得清楚的:`LIB_DIR` < `FETCHER_DIR` < `ROOT_DIR`
  - 自检加了五条断言直接钉住这三层的关系和 `Assets` / `_logs` 的位置——再挪文件时,
    坏的是断言,不是你半个月后的数据
- **`Assets/` 按类型分子目录**:`estimates` · `charting` · `targets` · `news` · `options` ·
  `summary` · `misc`。写盘只有一个入口 `assetPath(文件名)`,归类规则表就在它旁边;
  九个文件里的 `path.join(OUT_DIR, ...)` 全部改掉
  - 仪表盘那边**一行都不用改**:`ingest/folder.js` 递归三层深,且靠文件名认数据、不看目录
- **日志与台账统一进 `Assets/_logs/`**:台账 `sources.txt` 搬过来,并新增
  `fetch-YYYY-MM-DD.log`——控制台看到的东西同时落一份到文件,不用再回终端翻滚动条
  (终端缓冲区总是刚好把关键那几行吃掉)
- **新增 `lib/migrate.mjs`**:每次启动做一次幂等迁移,把 `fetcher/Assets/`、
  `fetcher/lib/` 下的配置与台账、以及 `Assets/` 根下平铺的老文件搬回正确位置;
  同名冲突保留较新的那份,输的那份**挪进 `_to_delete/` 而不是删掉**——
  那是授权数据,替人做"永久删除"的决定不合适(顺带这条路径不依赖删除权限)。
  上一轮真实抓到的 17 个文件因此不会白抓
  - 实跑时抓到自己一个 bug:原来"目标更新→删源"和"跨盘符→复制兜底"写在同一个 `try` 里,
    删除一旦失败就掉进兜底,**拿旧文件盖掉了新文件**。现在三种情况分开写,
    判断谁该赢的代码不再兼职做兜底
- **入口 bat 收敛成根目录一个 `run-factset.bat`**:双击就跑,首次运行自动装依赖。
  `fetcher/run-factset.bat` 退役(挪进 `_to_delete/`)。名字沿用你熟的那个,位置换到看得见的地方;
  留两个同名入口的后果是它们各自漂移,最后"我明明跑了"和"脚本明明没跑"会同时成立
- 期权两处修正:
  - 候选 slug 里补上 **`options-montage`** 并排第一——这是实测点顶层 Options 页签的落地地址;
    原来那四个猜测(`options` / `option-chain` / `options-chain` / `derivatives`)一个都不对
  - 记住的目录式地址**不再自动补代码**(上一版会去访问从未验证过的 `.../options-montage/NVDA-US`);
    改为给出两个候选,先原样再补代码
  - **先输代码,再读表。** 原来是先读、失败才输代码——同一会话里页面很可能还停在上一只股票的链上,
    那样读到的是一张完整、合理、但属于别人的表,是最难发现的一类错。读完再核一次页面有没有该代码字样
- 自检 58 → 80 项

## v16.1 (2026-07-29) —— 修:期权页找错了地方

- **期权链是与 Company 平级的顶层页签(和 Charting 一样),不是 Company 里的子页签。**
  之前按 `navigator/company-security/options/...` 去找,方向就是错的,再怎么加候选也不可能命中
- 新增 `fetcher/lib/options-url.mjs`,把"去哪个地址"和"怎么解析表格"彻底分开。地址优先级:
  1. 环境变量 `FS_OPT_URL`(可用 `{ticker}` 占位)——把地址栏里的真地址贴进来,一次搞定
  2. 上一轮探测成功后**记住的**地址(`fetcher/.options-url`,已加进 .gitignore)
  3. 顶层候选 slug 逐个试(`/workstation/options|option-chain|options-chain|derivatives/`)
  4. 都不中就回首页**点那个叫 Options 的顶层页签**现场探测,并记住落地地址
- 顶层页签不带公司上下文,所以落地后会像 Charting 那样把代码打进页面自己的搜索框再抓一次
- 失败信息重写:直接说清"它是顶层页签"+ 怎么用 `FS_OPT_URL` 一句话解决,而不是只说"没找到表格"
- 自检 52 → 58 项(新增地址模板展开/回填/候选集不含 company-security 的断言)

## v16 (2026-07-29) —— 仓库结构重构(功能不变)

这一版**没有改任何计算口径、阈值或界面**,只改代码怎么放。目的是:想改某个功能时,
不用在 2554 行的单文件里翻,而是照着功能域直接打开对应文件。

- **仪表盘:源码拆模块,构建回单文件**
  - 交付物仍然是零依赖单文件 HTML(双击可开、GitHub Pages 可挂),但源码按功能域拆进 `src/`:
    5 个 CSS 模块 + 28 个 JS 模块(`core` 工具/状态/i18n · `ingest` 每种输入文件一个 · `valuation` 估值区间与波动率 ·
    `pressure` 三轨 · `direction` 走向概率 · `render` 渲染 · `app` 启动)
  - 新增 `tools/build.mjs`:按 `src/manifest.json` 的顺序**原样拼接**,不打包、不转译、不改写一个字符
  - 验收证据是可复现的硬指标——**构建产物与重构前的单文件逐字节相同**(`cmp` 校验通过),
    而不是"测试都过了应该没事"
  - 代价只有一条:改完 `src/` 要跑一次 `npm run build`。忘跑不会静默出错——
    `npm test` 的第 [0] 组断言先比对产物与 `src/` 是否同步,不同步直接红(96 → 99 项)
  - 两条护栏写进构建器:产物必须**恰好一个 `<script>` 且不是 `type=module`**
    (否则 `page.evaluate` 够不到顶层的 `calcRange` / `pressureLevels` / `state`,99 项断言全废)
- **fetcher:一个抓取步骤一个文件**
  - 1320 行的 `factset-fetch.mjs` 拆成 `lib/`(config · log · ledger · browser · tickers · markets · menu ·
    registry · health · companies · round · selftest)+ `steps/`(estimates · price · charting · targets ·
    short-interest · news · options · ticker)+ 一个 CLI 入口
  - 按"抓取步骤"切而不是按技术层次切,是因为 FactSet 改版时坏掉的一定是某一个具体步骤:
    台账 `sources.txt` 的**失败环节**列点名哪一步,`steps/` 下就有同名文件
  - `--selftest` 在入口处短路,主流程全部动态 `import`——否则菜单模块会在自检前创建 readline,无终端环境下会挂住
- **目录与文档**
  - `test-app.mjs` 移入 `tests/`;新增 `docs/ARCHITECTURE.md`(改哪个功能开哪个文件的对照表)
  - 根目录补齐 `package.json` 脚本:`build` / `build:check` / `test` / `test:fetch` / `fetch`
  - `.gitignore` 增加 `node_modules/`
- 验证:构建逐字节一致 · 仪表盘断言 99/99 · fetcher 自检 52/52 · fetcher 全量 ESLint 0 error 0 warning

## v15 (2026-07-29)
- 压力位面板由双轨扩为**三轨:技术面 × 估值面 × 期权面**
  - 新增**期权轨(唯一前瞻的一轨)**:未平仓量 OI 墙
    - 口径取 **OI 而非成交量**——OI 是存量持仓,是真的"墙";成交量是流量,当天冲掉就没了
    - 只取**最近月 + 次月**两个到期日,**分开展示、分开加权(1.0 / 0.6)而不是相加**:
      快到期的墙钉得紧但很快归零,两个月后的墙软但活得久,加总会把两件事读成一件
    - 只看现价 **±25%** 内的行权价;单价位 OI 需 ≥ 窗口均值的 1.5 倍且 ≥ 500 张才算墙;每个到期日最多 4 面
    - **方位一致性折价**:上方要看涨占优、下方要看跌占优才算真墙(占比 ≥60% 记 1.0,40–60% 记 0.5,否则只记 0.25)——
      方向不一致的大 OI 更可能是别人的对冲腿,不该当阻力读
    - **max pain 只作磁吸位参考标注,不计入压力位打分**(该指标本身有争议,不该混进强度)
  - 合流打分改为**取各轨最高 + 重合加分**(两轨 +25,三轨再 +20,上限 100),
    而不是求和或平均——一面很硬的期权墙不该因为叠了一条平庸的技术带被拉低
  - `kind` 由 `both` 改为 `multi`,芯片上直接写明是哪几轨重合(如"技术+期权")
  - 侧向密度图新增 OI 墙竖条(最近月实心、次月半透明)
- 新数据源 `{ticker} Options.csv`(列 `asof,expiry,strike,call_oi,put_oi`);
  同一 (到期日, 行权价) 只保留 asof 最新的一条(OI 是存量,旧快照没有意义)
- fetcher v2.6:每家公司新增第 6 步**期权链抓取**(每 ticker 8 步)
  - 候选页面路径逐个试,表格按**表头语义**定位列(Strike 左边最近的 Open Int = 看涨,右边第一个 = 看跌),
    同时兼容"每行一个 Type"的单栏排布;支持 5 种到期日写法,只到月份时取当月第三个周五
  - 认不出的日期宁可丢行也不瞎猜;失败区分"没有 Strike 表头(停在概览页)"与"有表头没解析出行(改版)"
  - 无期权权限或路径变更只登记台账、**不阻断整轮**;自检 35 → 52 项
- `test-app.mjs` 71 → 96 项:新增期权轨断言(到期日选取与过期排除、±25% 窗口、
  墙的判定门槛、方位一致性、max pain 手算校验、三轨合流加分、无链时的退化)

## v14 (2026-07-29)
- 新增**上行压力位 / 下行支撑位**面板(技术面 × 估值面双轨,两轨重合处才是高置信位置)
  - 技术轨:回看 730 天,48 档分箱累积**筹码分布**(有成交量时按量加权,无量时按停留天数),
    权重按 365 天半衰期时间衰减;密度峰值向两侧扩张成"价位带"(阈值 = 峰值 55%),
    相邻带自动合并(避免匀速区被切成一串碎带);摆动高低点(±8 根)计入该带的"触及次数"
  - 估值轨:**基准 EPS × 各正 P/E 分位**反推价位——刻意只动一个变量(估值),
    与核心区间(EPS 情景 × 分位,两个变量同时动)是两个问题,不要混读
  - 重合判定:估值线落进技术带 → 合并为"双轨重合",强度 +25(上限 100)
  - 强度 0–100 = 60×密度占比 + 25×触及次数(封顶 5) + 15×新近度;**仅在同一只股票内部可比,跨公司无意义**
  - 现价正落在密集带里会单独提示(上下都黏,突破前方向意义有限)
  - 侧向密度图:纵轴价格、横条密集度,叠加价位带底纹、估值虚线与现价线
- Charting 解析新增**成交量列**识别(`XXX-US - Volume` 或裸 `Volume`);
  价格序列择优改为 `setPriceHist`——带量序列按 1.5 倍长度计分,稍短但有量的胜出
- fetcher v2.5:Charting 导出前尽力开启成交量序列(工具栏开关 / Studies 菜单两种入口);
  **以导出文件的表头为准判定是否真的拿到量**,而非以"点击是否成功"为准;
  拿不到量只降级不阻断,并打印手动添加 Volume 序列的指引;`chk` 体检新增成交量覆盖统计;
  `FS_NO_VOLUME=1` 可关掉尝试;自检 31 → 35 项
- `test-app.mjs` 37 → 71 项:新增压力位引擎断言(口径选择与降级、带合并无碎片、摆动点触及计数、
  双轨重合加分、inBand 判定、上下各 3 档、样本不足与缺价的退化路径、约 20 个新词条中英对齐)

## v13 (2026-07-29)
- 情绪面新增**第四条腿:新闻关键词打分**(StreetAccount 近30日标题;利多/利空词表计数,`(利多−利空)/max(4,总数)` 截断到 ±1)
  - 四腿权重重排:目标价3月动量 0.35 · 买入评级占比变化 0.15 · 空头持仓趋势 0.25 · 新闻 0.25(缺腿时按 wsum 重新归一)
  - 31–90 日的新闻只作为"前期热度"背景显示,不进得分;近30日无新闻记 0 分并注明
  - 新数据源:`{ticker} News.csv`(fetcher 逐轮累积去重,列 `date,ids,headline`)
- **情景矩阵与核心区间统一口径**(修掉一处真实缺陷):
  - 新增 `epsScen()` 清洗层:低/高缺失→退回均值;高低颠倒→自动对调;情景 EPS ≤0(亏损)→退回均值,不再乘出负价格
  - 新增 `pePos()`:只有为正的 P/E 分位才参与计算与显示(负 P/E 乘出来的"价格"无意义)
  - 矩阵改为消费 `calcRange` 的同一份情景,并把"悲观×P25 / 乐观×P75"两个角标出来 + 图例,让读者一眼看出核心区间就是矩阵的两个角
  - 数据可疑时在矩阵下方**显式告警**(对调/均值出界/亏损情景),不再悄悄替换数字
- fetcher v2.4:
  - 每家公司新增第 5 步 **StreetAccount 新闻标题**抓取(虚拟表格按 bounding rect 重建行;临时放大视口以渲染全部行);每 ticker 7 步
  - **`sources.txt` 台账升级 v2(9 列,故障定位)**:所有预期产出先登记,每次尝试写 OK/FAIL + 时间戳 + **失败子阶段**(导航/等待页面/切换 Report Type/定位表格/解析行/写文件)+ 错误摘要 + 页签路径 + URL
  - 菜单新增 `chk` 数据体检:把台账与文件真实修改时间交叉核对,专门揪出"台账没报错但文件已陈旧"的静默失效
  - 自检 14 → 31 项
- 新增 `test-app.mjs`:无头浏览器 37 项断言,校验乘数法恒等式、矩阵与区间同源、各类护栏

## v12 (2026-07-29)
- 新增**情绪面**自动信号(第四个可覆盖打分项):
  - 目标价 3 月动量(50%)+ 买入类评级占比变化(20%)+ 空头持仓趋势(30%,占流通盘%下降=利多;单点时按水平轻打分)
  - 数据源:`{ticker} Targets Ratings.xlsx`(Targets & Ratings 月度历史)与 `short-interest.csv`(逐日累积)
- 六维权重调整:基本面0.30 技术0.15 情绪0.15 宏观0.15 行业0.15 流动性0.10
- fetcher v2.3:每家公司新增两步——Targets & Ratings(History 视图月度表)与 Ownership 页空头持仓;自检 14 项

## v11.1 (2026-07-29)
- 行业面自动信号升级为双腿:行业ETF相对强弱(价格面)与**同行修正动量**(盈利面)各占一半
  - 同行 = 清单里其他有 Estimate History 修正数据的公司;走向面板新增"同行分组"输入框可手动限定(如 AMD-US, AVGO-US)
  - 芯片分别展示两腿得分,下拉框展示合并后的自动分

## v11 (2026-07-28)
- 走向概率:宏观/行业/流动性三项由手动打分升级为**市场数据自动信号**(可手动覆盖)
  - 宏观 = 基准ETF趋势(MA200)+ 20日已实现波动一年分位 + 利率ETF 3月趋势
  - 行业 = 行业ETF 对基准的 3 月相对强弱 + MA200
  - 流动性 = 高收益债ETF 3月趋势 + MA200(信用利差代理)
- fetcher v2.2:新增 markets.txt(BENCH/SECTOR/CREDIT/RATES 四角色,默认 SPY/SOXX/HYG/IEF),每轮自动拉取市场日线 `_MARKET-角色 SYMBOL Daily Charting.xlsx`;菜单新增 `mkt` 命令;浏览器改为确认后启动并全程显示阶段提示

## v10 (2026-07-28)
- 走向概率模块:六维信号(修正动量/估值位/技术/宏观/行业/流动性)→ 情景加权蒙特卡洛;自动信号 + 手动打分
- companies.csv 合并语义修复(空字段不覆盖已有数据)
- Estimate History 文件名含 FY2 时自动写入第二财年

## v9 (2026-07-28)
- FY1/FY2 双财年一致预期支持;NTM PE band 由真实 FY1 序列驱动

## v8 (2026-07-27)
- 新文件类型:Charting(真实价格序列 → 波动率)、Price Summary(实时价)
- 文件归属解析:文件名 ticker > 公司全名 > 价格匹配;两段式导入

## v7 (2026-07-26)
- 连接文件夹:整夹扫描、按修改时间导入(最新生效)、跨会话记忆

## v6 (2026-07-26)
- 中/英双语切换;方法对比面板(乘数法/波动率/52周/分析师目标价同轴)

## v5 (2026-07-25)
- Snapshot 文件支持(收盘价/52周/目标价);Estimate History 固定财年口径保护(仅取最近24个月)

## v2–v4 (2026-07-24)
- FactSet 公司模型 xlsx 直接导入;Valuation 区块反推现价;现价来源分级

## v1 (2026-07-24)
- 首版:乘数法区间(EPS 情景 × 历史 PE 分位)、情景矩阵、PE 历史带、公司总览
